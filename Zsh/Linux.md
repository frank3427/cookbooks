# Linux

## Installation

### YUM

```sh
sudo yum check-update
sudo yum -y install zsh
```

### APT

```sh
sudo apt update
sudo apt install -y zsh
```

## Configuration

```sh
sudo chsh $USER -s /bin/zsh
```

```sh
sudo su - $USER
```
